name: 'Notify Slack Action'
description: 'Send workflow status notifocations to Slack'
author: 'RavSam'
inputs:
  status:
    description: 'Job Status'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
